# AI engine (pick one)
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# Local SQLite is the default via `pnpm dev`.
# Override with Postgres if needed:
# ANALYTICS_DATABASE_URL=postgres://localhost/agenthq

# Public origin (screenshots / share links)
# APP_ORIGIN=http://localhost:8080
